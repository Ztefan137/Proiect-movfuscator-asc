.data
  n: .long 6
  s: .long 0
.text
.global main
main:
  mov $1,%ecx
  mov n,%eax
  mov $0,%ebx
for:
  cmp %eax,%ebx
  jae exit
  add %ecx,%ebx
  inc %ecx
  jmp for
exit:
  mov %ebx,s
  mov $1,%eax
  mov $0,%ebx
  int $0x80
