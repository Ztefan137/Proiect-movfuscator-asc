.data
.text
.global main
main:
  mov $0,%eax
  mov $1,%ebx
  cmp %eax,%ebx
  jbe egale
diferite:
  mov $0,%ecx
  jmp exit
egale:
  mov $1,%ecx
exit:
  mov $0,%eax
  mov $1,%ebx
  cmp %eax,%ebx
  jne egale2
diferite2:
  mov $0,%ecx
  jmp exit2
egale2:
  mov $1,%ecx
exit2:
  mov $0,%eax
  xor %ebx,%ebx
  int $0x80
