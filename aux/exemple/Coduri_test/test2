.data 
  v: .long 1,2,3,4,5
  n: .long 5
  s: .long 0
.text
.global main
main:
  lea $v,%edi
  mov $0,%ecx
  mov n,%eax
  mov $0,%ebx
loop:
  cmp %eax,%ecx
  je exit
  add 0(%edi,%eax,4),%ebx
  inc %ecx
  jmp loop
exit:
